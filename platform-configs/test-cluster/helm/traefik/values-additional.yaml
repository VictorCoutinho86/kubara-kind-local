traefik:
  api:
    basePath: /traefik
  ingressRoute:
    dashboard:
      enabled: true
      entryPoints:
        - websecure
      matchRule: Host(`172.20.0.3.traefik.me`) && PathPrefix(`/traefik`)
      tls: {}
